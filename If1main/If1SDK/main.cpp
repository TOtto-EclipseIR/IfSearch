#include <QtCore/QCoreApplication>
#include "IfSearch.h"

int main(int argc, char *argv[])
{
	IfSearch a(argc, argv);
        //a.init();
        //a.start();
        //a.run();
        return a.exec();
}
